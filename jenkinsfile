pipeline {
    agent { label 'new_node' }
    stages {
        stage('build') {
            steps {
                script {
                    sh 'docker-compose up -d --build'
                }
            }
        }
    }
}